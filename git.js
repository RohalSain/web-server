Git setting::
sudo apt-get install git
git config --global user.emial "rohalsainia94@gmial.com"
git config --global push.default matching"
git config --global alias.co checkout
git init --> for git intialize in the project
ls-a--> to see hidden files of the Git
git status --> to check chnages made in the project
git add. // to add to git 
git rm --cached -r node_modules // for changes revert
.gitignore // is used to ignore the files which we want to ingnore
git commit -a -m "Updated language" a flag indicate the file 
 m-> indicate message 